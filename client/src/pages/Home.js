import React from 'react';
import PhotoList from '../components/PhotoList';

const Home = () => {
  return (
    <div className="container" sx={{ width: 1 }}>
      <PhotoList />
    </div>
  );
};

export default Home;
